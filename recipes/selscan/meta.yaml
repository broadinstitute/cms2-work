{% set version = "1.3.0a03" %}
{% set sha256 = "208b0e24200fc0596d8ebaaa6bfe34a7a598319cebe2b1a576e3d9db9af1e30d" %}

package:
  name: selscan
  version: {{ version }}

source:
  url: https://github.com/notestaff/selscan/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - make
  host:
    - gsl
    - zlib
    - boost=1.74.0
  run:
    - boost=1.74.0
    - gsl
    - zlib

test:
  commands:
    - selscan 2>&1 | grep -qF 'selscan'

about:
  home: https://github.com/szpiech/selscan
  license: GPL-3.0
  license_family: GPL
  summary: a program to calculate EHH-based scans for positive selection in genomes

extra:
  identifiers:
    - biotools:selscan
    - doi:10.1093/molbev/msu211
  skip-lints:
    - should_not_be_noarch_source
